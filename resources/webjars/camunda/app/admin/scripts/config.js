window.camAdminConf = {
    customScripts: {
        ngDeps: ['ssoLogout'],

        deps: ['ssoLogout'],

        // RequreJS path definitions
        paths: {
            'ssoLogout': 'scripts/ssoLogout'
        }
    }
};
