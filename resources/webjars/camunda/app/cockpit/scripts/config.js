window.camCockpitConf = {
    customScripts: {
        ngDeps: ['ssoLogout'],

        deps: ['ssoLogout'],

        // RequreJS path definitions
        paths: {
            'ssoLogout': '../ssoLogout'
        }
    }
};
